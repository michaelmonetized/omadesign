# 0.5.4 integrated graphics and Layout release validation

Status: integrated source, final portable artifacts and native runtime checks passed. Publication and production deployment are recorded separately below.

## Source and scope

0.5.4 starts from published 0.5.3 source `6e7fd237d499166c3ea83032f12a67634130fc25` and integrates the Design Tools preview plus the previously separate native Layout work. Native external clipboard paste, Omarchy clipboard-history support and cloud sharing/review remain present.

The merge keeps the 0.5.3 clipboard modules and transports Layout variables, remaps component links, and preserves the placement of copied subtrees extracted from rotated frames. Pixel selection tint now follows rotated images. Regression tests cover these combined workflows.

Full user-facing changes and limits are in the [release notes](../blog/0.5.4-more-control-on-the-canvas.md) and [manual](../MANUAL.md).

## Build policy

Desktop tests, builds and packaging run locally. `scripts/release.sh` builds ARM64 and x86_64 with Zig and rejects glibc requirements above 2.35 or external native codec dependencies. GitHub coordinates source/release hosting; Blacksmith is used only for website CI. Queued hosted jobs do not count as executed checks.

## Validation evidence

Local integration evidence is stored at `/home/michael/Documents/Omadesign-QA-0.5.4-20260920/`. It includes the recoverable pre-merge preview snapshot, test/build logs, native recordings, packaged application checks and website verification.

- `cargo test --locked --offline`: **531 passed, 0 failed, 5 ignored**. The ignored cases are existing optional fixture/interoperability tests.
- Native `egui-winit` clipboard bridge: **4 passed**.
- Formatting and working/index diff checks: passed.
- Clippy completed successfully. Non-fatal style/complexity warnings remain; this is not a zero-warning claim.
- Native pointer/key recordings: 510 gradient/grouping frames and 450 chroma frames, with **zero unresolved input targets**. Saved projects retain edited stops and conic strokes, grouping/ungrouping, transparent background pixels, opaque subject pixels, and partial edges after Apply/Undo/Redo/Cancel.
- Native clipboard observer: **12 checks passed** on an isolated compositor. Text, PNG, Command forwarding, image-file URI, SVG source/file, Omarchy history image/text/SVG/screenshot, internal position with Undo/Redo, and native Save. Saved pixels, editable text and vector shapes were inspected.
- Portable ARM64 package installed under an isolated prefix; binary hash matched and native WGPU captured the gradient scene. The same packaged binary is installed at `~/.local/bin/omadesign`, reporting **0.5.4**. The prior 0.5.3 executable and launcher metadata were backed up before replacement.
- Final installed ARM64 binary also passed native clipboard-history PNG, editable text, internal raster placement, Undo/Redo, Save and a fresh WGPU reopen. Saved pixels and artboards were inspected; test windows/compositor were closed.
- Exact packaged x86_64 archive verified on **hpeliteclient**: version, SHA-256, ELF, glibc ceiling, document inspect/convert, Design and Pixel dialogs, and reopening the new gradient fixture plus a saved 0.5.3 clipboard document. Four native WGPU screenshots passed visual review. The machine's existing installation was unchanged. This establishes startup/render/reopen, not a separate x86 keyboard-interaction run.
- Both builds completed locally. The first build runner ended with SIGTERM after compilation and before packaging; the cause was not established. The completed binaries were preserved. The exact `package()` implementation from `scripts/release.sh` then ran successfully, including both glibc and native dependency checks.

## Final artifacts

| Artifact | SHA-256 |
| --- | --- |
| `omadesign-0.5.4-aarch64-unknown-linux-gnu.tar.gz` | `c0733d06dbf493c13c2f4014c91ac360da0bc13eecd54465119514b60eede6eb` |
| `omadesign-0.5.4-x86_64-unknown-linux-gnu.tar.gz` | `022012be808c9513524a3439e37c46c5926da7453d65ce80f0777302904829e0` |

Packaged executables:

- ARM64: `8f5779f67dd9b93e879c6cf25e55c91e43b361939ad3d1559b530594127d249b`.
- x86_64: `7096f8fbf6dd6914f50853b3b32416dc5717b99e4f182b5018e95ae516a8212b`.

Both require at most **GLIBC_2.35** and retain the bundled native codec dependencies. Archive checksum files accompany both downloads.

## Website

- Frozen Bun dependency install, **10 tests**, production build and TypeScript check passed.
- Desktop and 390px mobile browser checks passed with zero page errors in the clean verification session: release content, historical 0.5.3 article, screenshot loading, no horizontal overflow, cloud intro links and Escape behavior.
- Fixed historical update routes displaying the latest article by separating the parent route from the index.
- Vercel project `rootDirectory` is now `site`; outside-root source access remains enabled for documentation imports. Root-linked local/Blacksmith CLI commands now build the actual website. Build Output API version 3 contains 61 static files. `/install` exactly matches `scripts/install-remote.sh`, SHA-256 `b4487e923c48eaac915b5762a2261a44ec997204267985b124801a65fba0ed01`.
- Static client output was checked for configured server secret values, with no matches. Production environment files remain ignored.
- Website copy uses synthetic native screenshots. No personal chroma-key photo is published.
- These checks preserve existing cloud routes and backend tests; they do not claim a new authenticated production cloud workflow test.

Public release and final production deployment identifiers are added after their live verification.

## Preserved human QA

Michael accepted the installed 0.5.0 build on 2026-09-12 with SHA-256 `69c35f6cc3f4397a883798172a0d654cb05289e4715d0678db7822c823e6791d`. Its evidence at `/home/michael/Documents/Omadesign-QA-0.5.0-20260912/placement-fix/` remains unchanged. That acceptance lifted the old version freeze and is not a claim of new human acceptance for 0.5.4.
