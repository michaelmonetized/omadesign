# 0.5.3 x86_64 packaged runtime validation

Status: **final packaged artifact passed**.

Completed on 2026-09-20 using final locally built archive `dfd49bdd…`,
after the key-hint correction. The archive checksum, executable version,
headless conversion, exact fixture pixels, and native WGPU capture were
all repeated against these final bytes.

| Check | Result |
| --- | --- |
| Host | `hpeliteclient`, x86_64 Linux, AMD GPU with Radeon Vulkan ICD |
| Display | Existing Hyprland Wayland session, `wayland-1` |
| Archive checksum | Passed against packaged `.sha256` |
| Packaged version | `omadesign 0.5.3` |
| Headless import/export | Four-layer SVG converted to 320 × 240 RGBA PNG |
| PNG contents | Exact background, blue rectangle, and orange circle pixel samples match fixture |
| Native WGPU runtime | Actual packaged app imported the SVG, rendered its native UI, and captured 1200 × 800 PNG |
| Native process exit | `0`; no Omadesign process remained |
| Existing user installation | SHA-256 unchanged; nothing installed |

The native screenshot was retrieved and visually inspected: the artboard
shows the blue rounded rectangle, orange circle, green curve, and light
blue background; the layer sidebar lists all four imported layers.

## Final tested hashes

```text
archive:
dfd49bdd9250741235ef5456b52b4544269b3a280de27411182a9d825d7673b0

packaged executable:
34a2f84fdec603b2dcd65f47a5d2508ebd37550b7496b71588d7fcba9dde274f

native-wgpu.png:
76178b615bbd4d32c95c9f0e38711dc8c4f85522349661bd99d76072f2427a20

converted.png:
ed7b732595ff248beacd21d1f0ff0b19abfd076985e8c118c20f07251b1a898f
```

## Evidence and method

Remote isolated directory:
`hpeliteclient:/tmp/omadesign-0.5.3-x86-qa.XnmRWB`.

Final local evidence:
`/home/michael/.local/share/omadesign/qa/0.5.3/x86-final/`, containing `native-wgpu.png`,
`converted.png`, `version.txt`, `inspect.json`, `native.log`,
`native-exit-status.txt`, `packaged-binary.sha256`, and
`user-install-before.sha256`.

The temporary local and remote `interim-8d9abd0a/` subdirectories retain evidence
from the earlier successful runtime test. The remote subdirectory also
retains that earlier archive and checksum; those are not the final release.

The archive was checksum-verified before extraction. Commands used the
packaged executable with isolated XDG config/data/cache directories:

```sh
./omadesign --version
./omadesign --convert fixture.svg --output converted.png
./omadesign --inspect fixture.svg
./omadesign --shot-file fixture.svg --out native-wgpu.png --size 1200x800
```

The screenshot mode explicitly uses `eframe::Renderer::Wgpu`, waits for
the native scene to be ready, captures the real viewport, and exits.
This verifies x86_64 artifact startup, codecs, and native rendering; the
clipboard interaction matrix is recorded separately in the main
[0.5.3 validation record](0.5.3.md).
