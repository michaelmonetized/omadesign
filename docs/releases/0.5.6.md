# Omadesign 0.5.6 release validation

Status: published as the stable latest release, deployed to production, and
installed from the public installer on Michael's machine on 2026-09-21 EDT
(2026-09-22 UTC).

- Release: https://github.com/michaelmonetized/omadesign/releases/tag/v0.5.6
- Source/tag: `d4777ba27ace68ac3166f2f9f82524ed0743b969`
- Website: https://omadesign.app
- Production deployment: `dpl_J7qZuwhSpD3HNrdfbRjmw89PdmWf`
- Deployment URL: https://omadesign-iq6y3cfzj-hustle-launch.vercel.app
- Installer exercised: `curl -fsSL https://omadesign.app/install | sh`

## Scope

- Three-column welcome screen with recursive local `.oma` discovery, natural-aspect
  masonry thumbnails, mode filtering, recovery, selection and brand-project navigation.
- Project discovery through `.omabrand`, nested project previews, and shared-team
  visibility only for an authenticated account with shared projects.
- Vector/Layout templates and blank sizes, Raster size setup, Photo folder/image
  entry, and brand-editor creation. Motion remains available through mode switching.
- Omarchy default-agent prompts for learning and creating, self-hosted Markdown
  documentation, version-matched offline docs and the bundled creation skill.
- Discord links in the app and website; stable 0.5.6 metadata, update notes and
  checksum-verifying ARM64/x86_64 installer support.
- Existing workspace, native chooser, Photo gallery, editing, privacy and updater
  work carried forward from the local nightly source tree.

User-facing changes: [release notes](../blog/0.5.6-your-work-ready.md).
Interaction agreement: [welcome screen](../welcome-screen.md).

## Native verification

- Full library suite: **586 passed, zero failed, five ignored**.
- Three of the ignored external PDF interoperability/import tests were run separately
  and passed. The remaining two need private RAW/PDF fixture inputs.
- Focused catalog, brand, template, workspace, selection, recovery and Team checks
  passed. Vector and Layout choosers create real editable documents in the requested
  mode and keep a thumbnail row and actions visible at 960x640 and 1600x1000.
- Final portable ARM build rendered the real home catalog at 1920x1080 and 960x640
  using native WGPU: 95 documents, two projects and zero scan warnings. One oversized
  preview uses the explicit bounded-preview fallback; its file remains openable.
- Native ARM64 and x86_64 under QEMU with Ubuntu 22.04/glibc 2.35 inspected and
  rendered the editable public poster without import notes. Both produced the same
  900x1125 PNG, SHA-256
  `1509067c5156a8e3e539624bcf33b672fa79d72f637e92d44b4d8afd2c8bffab`.
- Both packages report 0.5.6, target the correct architecture and require no glibc
  newer than 2.35. Bundled skill, docs, licenses and source notices match the source.
  Build input verification recorded zero source drift.

## Published artifact hashes

| Architecture | Archive SHA-256 | Installed executable SHA-256 |
| --- | --- | --- |
| ARM64 | `19306b8cbd876129c518d327ad2ba7a082f276713acd763e2f281494e024e303` | `39f5b683da51f5944912151b452a461726ab570b3aea25f1099d842f5b81ed41` |
| x86_64 | `d2fe1775b86288f4e10c736b7e3c9a0340c4d813fd994cc403f08efd2de59bcf` | `f56872b52fa97a7dadeeb46a057ad185b2dfb16cab8882d28ef07a58ff4dcc73` |

Both archives were downloaded again from the public GitHub release. Their bytes
match the local verified artifacts and their published checksum files. GitHub's
latest stable release and v0.5.6 tag resolve to the source commit above.

## Website and cloud verification

- Sixteen site/Convex tests, TypeScript checking and the local Vercel production
  prebuild passed. The final deployed output includes the corrected AI workflow
  README and downloadable kit; all 18 kit artwork files retain their hashes.
- Live home, update article, manual, cloud page, installer, docs index, five Markdown
  documents, creation skill and workflow ZIP returned HTTP 200. Both architecture
  download links returned 200. The public installer matches its source exactly;
  all six docs/skill manifest hashes match source and packaged copies.
- Desktop/mobile browser verification covers release links, navigation, Discord
  and page errors/overflow; receipts and screenshots are retained below.
- Team metadata is deployed to production Convex `healthy-buzzard-921`; the live
  function schema includes shared-project metadata and anonymous project listing
  rejects access. No real teammate identity is signed in here, so a two-account
  collaboration session remains unverified. Registration is exposed, not a waitlist.
- The supplied Discord invite is valid for the omadesign server. Its API reports
  expiration at `2026-10-21T20:09:30Z`.
- The Vercel preview and local production checks passed. Hosted Blacksmith site CI
  remained queued at publication; queued runs are not counted as passing checks.
  Production was deployed from the verified local prebuild. Automatic Vercel
  deployments for master/main are disabled by the existing site configuration.

## Local installation and preservation

The public installer installed the verified ARM64 executable at
`~/.local/bin/omadesign`. Its SHA-256 matches the release table; all 24 installed
skill, documentation and license files match the package. A running native 0.5.6
window was verified by executable path, hash, version and screenshot.

Only `omadesign.desktop` remains in the application list. Both native document MIME
types default to it. Preview/nightly wrappers, launchers and installation prefixes
were moved into a private QA backup outside the application directories. Newer
nightly preferences, palettes, font recents and recent documents were carried
forward while preserving existing entries and stable recovery data.

The old nightly process has unsaved palette/artwork changes and displayed its save
prompt after a normal close request. That session remains alive to preserve the
unsaved work; its launcher is removed and all new launches resolve to 0.5.6. No
unsaved changes were discarded. Personal artwork and the in-progress `media/logo.svg`
were verified unchanged and remain outside the release commits.

## Evidence and release policy

Persistent local receipts, complete hashes, logs, native/browser screenshots,
public downloads and prior-installation backups:
`/home/michael/Documents/Omadesign-QA-0.5.6-20260921/`.
Key receipts: `portable-verification.json`, `public-downloads/verification.json`,
`github-release.json`, `release-tag.json`, `public-install.log`, `installation.json`,
`installed-content-verification.json`, `running-apps.json` and
`site-http-validation.json`.

Desktop tests, compilation and packaging ran locally. Blacksmith is website-only.
No desktop compute, caches or build artifacts were delegated to hosted runners.

Michael accepted installed 0.5.0 on 2026-09-12 with SHA-256
`69c35f6cc3f4397a883798172a0d654cb05289e4715d0678db7822c823e6791d`.
That accepted build and its evidence remain unchanged; these automated checks are
not a new claim of human acceptance.
