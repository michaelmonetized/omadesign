# 0.5.5-nightly.1 workspace release validation

Status: local release tests, portable packaging, native ARM64/x86_64 runtime
checks and local nightly installation passed. Publication uses the verified
archives listed below.

## Source and scope

Version **0.5.5-nightly.1**, tag `v0.5.5-nightly.1`, builds on 0.5.4 with the
workspace changes in the [changelog](../../CHANGELOG.md). It is a nightly
prerelease; the website's stable release remains **0.5.4**.

- File choosers run on a worker so the native event loop remains available to
  answer compositor pings while the chooser is focused. The chooser belongs to
  the document that opened it; cancellation or a changed destination does not
  apply a result. An untitled document's pending close/navigation resumes only
  after saving succeeds.
- Photo folders populate a full-width thumbnail library without a filename
  picker. Thumbnails include saved development settings, crop and rotation;
  opened photos reflect current edits. Folder changes cancel previous preview
  work. The existing 10,000-file folder limit remains; retained preview pixels
  are bounded and only visible cards keep GPU textures. Opening an image still
  loads its full source for editing.
- Photo and Layout start without an artboard. Saved Layout documents explicitly
  preserve an artboardless workspace; legacy documents retain their fallback
  artboard behavior. Older releases do not preserve the new artboardless state.
- Startup mode and start-screen page preferences persist independently,
  including remembering the last mode and the last New, Templates, Recent or
  Recovered page.
- Centered icon-only mode tabs use the requested Phosphor glyphs. Document
  previews are 64 × 64 logical pixels in the outermost left rail. The tools sit
  directly beside the canvas, with Photo's library or Layout's hierarchy between
  the document rail and tools. Arrange, Align and horizontal/vertical flip
  controls are in the right inspector.

## Build policy

Desktop tests, compilation and packaging run locally. `scripts/release.sh`
produces ARM64 and x86_64 portable packages and checks the glibc ceiling and
native dependencies. GitHub hosts source and release artifacts; Blacksmith is
reserved for website CI. A queued or unexecuted job is not a passed check.

## Validation evidence

Evidence is stored at `/home/michael/Documents/Omadesign-QA-0.5.5-nightly.1-20260920/`.

- `cargo test --locked --offline`: **551 passed, 0 failed, 5 ignored**. The ignored cases are existing optional fixture/interoperability tests. Binary and documentation test targets also passed.
- Formatting and diff whitespace checks passed. Final source review found no release-blocking issue in chooser ownership, startup persistence, live previews or sidebar ordering.
- The initial full test run caught an unthemed Photo shortcut test context after the toolbar moved inside the Photo workspace. The fixture now initializes the normal theme and Phosphor font; the targeted regression and complete suite passed. Production startup already initialized that font.
- `scripts/release.sh` completed locally for both architectures, including its **GLIBC_2.35** ceiling and bundled native codec dependency checks. Archive checksum verification passed.
- The exact ARM64 package is installed under `~/.local/share/omadesign-nightly/releases/0.5.5-nightly.1/`, available through **Omadesign Nightly** and `omadesign-nightly`. Installed and packaged executable hashes match. Existing nightly settings and the previous nightly executable are preserved.
- The installed ARM64 nightly rendered native WGPU views of document thumbnails, the automatically loaded Photo folder, artboardless Layout and the start screen. All four screenshots passed visual review. The temporary virtual display was removed and the original workspace restored.
- The exact x86_64 archive passed verification on **hpeliteclient**: archive/executable hashes, version, ELF, glibc ceiling, bundled codecs, inspection/conversion of current graphics and saved 0.5.3 clipboard documents, and native WGPU views of document thumbnails, Photo folder, artboardless Layout, start screen and the reopened gradient document. All five screenshots passed visual review. Photo used three synthetic fixtures. The existing installation and physical monitor/workspace state were unchanged; temporary output and QA processes were cleaned up. These checks establish startup/render/reopen, not a new x86 interactive chooser or persistence run.
- The earlier native Hyprland chooser observation remains historical evidence for the same chooser implementation: 14 ping/pong pairs during 21 seconds of chooser focus, maximum response 0.065 ms, no terminate/wait dialog. This was measured in the prior 0.5.4 development build, not repeated as a new nightly measurement. Its record remains in `Omadesign-QA-0.5.4-20260920/workspace-ui-202109/native-picker-responsiveness.json`.

## Artifacts

| Archive | SHA-256 |
| --- | --- |
| `omadesign-0.5.5-nightly.1-aarch64-unknown-linux-gnu.tar.gz` | `5ac262203545ea1356c9d5c61a5bad0526bd8bd42afacf19537c8ca96fae31c4` |
| `omadesign-0.5.5-nightly.1-x86_64-unknown-linux-gnu.tar.gz` | `c0844fcabc62e176f8a94a0cd3c48d7b028eb3c2112a50a139e423e495fbf7fb` |

Packaged executable SHA-256:

- aarch64: `68cba3ff950ef899e019bc2913129d0dddd255a12c6933dee37a4d87d099a20c`.
- x86_64: `ef1acce7c65bce9dc2d80c0cb73e77a5ec020d49ffb0abbfe2d46c97bc420e6a`.

Each archive includes the installer, desktop/icon/MIME metadata, source and license notices for bundled native dependencies, and its companion checksum file is published separately.

## Nightly publication

The release is prepared in [PR #82](https://github.com/michaelmonetized/omadesign/pull/82) for `nightly`. The stable branch, website version and default installer continue to point to 0.5.4. Website preview deployment passed; Blacksmith site CI was queued during native validation and is not counted as a passed check. No desktop build ran on a hosted runner.

The [nightly release](https://github.com/michaelmonetized/omadesign/releases/tag/v0.5.5-nightly.1) carries both archives and checksum files. To select this version explicitly:

```sh
curl -fsSL https://omadesign.app/install | OMADESIGN_TAG=v0.5.5-nightly.1 sh
```

## Preserved human QA

Michael accepted the installed **0.5.0** build on **2026-09-12**, SHA-256
`69c35f6cc3f4397a883798172a0d654cb05289e4715d0678db7822c823e6791d`.
Its evidence at
`/home/michael/Documents/Omadesign-QA-0.5.0-20260912/placement-fix/` remains
unchanged. That acceptance lifted the old version freeze; it is not a claim of
new human acceptance for this nightly.
