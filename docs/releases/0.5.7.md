# Omadesign 0.5.7 release validation

Status: published as stable/latest, deployed to production and installed from the
public installer.

Release: [v0.5.7](https://github.com/michaelmonetized/omadesign/releases/tag/v0.5.7),
tagged at `29276c59b0e5c6eda4b17534df1963c02f9686b3`.

## Scope

- Use the user-designated `media/logo.svg` on native Welcome/About, the desktop
  icon, website branding and the new homepage product film. Keep its artwork intact.
- Restore the product film near the end of the page with a fresh 32-second,
  60 fps native montage cut on beats 1 and 3 of a 120 BPM grid.
- Preserve the opening Cloud announcement, carousel and editing workflows.
- Add an isolated native capture catalog root and update the capture utility;
  normal home discovery is unchanged.

See [film provenance and reproduction](../site/2026-09-21-homepage-film.md) and
[user-facing notes](../blog/0.5.7-a-new-mark.md).

## Completed native checks

- Twelve focused welcome tests pass, including catalog-root isolation/switching,
  discovery, nested projects, thumbnail aspects, selection, creation targets,
  paintable Raster creation and Team sign-out privacy.
- ARM64 and x86_64 portable packages report 0.5.7, require no glibc newer than 2.35,
  and retain bundled RAW/JPEG/C++ dependencies and licenses.
- Native ARM64 and emulated x86_64 on Ubuntu 22.04/glibc 2.35 inspect and render
  the editable example. Physical and embedded docs/skill match source.
- The frozen desktop/package source manifest reports no drift. Capture-utility
  corrections and website/video editing are separate from the shipped executable.
- A real-home native welcome screenshot confirms the new mark and 0.5.7 label.

| Architecture | Archive SHA-256 | Executable SHA-256 |
| --- | --- | --- |
| ARM64 | `1f174519cf091e48dbcd7502e753591bbf8d124637dc8648c935f990d1a3deb8` | `3cb949e2c823a23f54963c8790140d77077940a344d2f37617a2d71f481b60ff` |
| x86_64 | `d33c05f012d5974cd50eb0125bbc8d93e92ff8e2d734450948e97872a7e83342` | `2d13fb408d18f8ad05c15cf54a38f900e765a16dfdaa8afcf1ad76723ac4c159` |

## Published artifacts and installation

- Both archives were downloaded from the public release and match their checksum
  files and the locally verified artifacts. GitHub identifies v0.5.7 as the
  latest stable release. The existing 0.5.6 assets were not replaced.
- `curl -fsSL https://omadesign.app/install | sh` completed successfully on this
  ARM64 machine. The installed and running executable matches the ARM64 hash
  above and reports `omadesign 0.5.7`.
- The previous stable welcome window closed normally; the new native Wayland
  window visibly shows the official mark and 0.5.7. No unsaved work was discarded.
- Exactly one Omadesign launcher remains, with `.oma` and `.omaphoto` defaults
  pointing to `omadesign.desktop`. Preferences, recent files, palettes and font
  recents match their pre-install hashes. Installed logo, docs and creation skill
  match source. The previous stable binary, icon and launcher are backed up.
- A final comparison of all 469 frozen desktop/package source files against the
  tagged commit found no drift from the package build inputs.

## Film and website validation

- MP4 and WebM fully decode as 1920×1080, 1,920 frames, 60 fps, 32 seconds, no audio.
  Every decoded MP4 frame matches its corresponding one-second source segment,
  verifying hard cuts at each integer second with no inserted transition frames.
- All 32 shots were reviewed at their start, middle and end. The new logo remains
  fully visible at maximum zoom and at the close. Included source windows contain
  no recorded input misses; unused failures remain documented in the manifest.
- MP4 SHA-256: `130ebf816667dff09e65d7cfd337faebf6c94d4bf0a067a2c39ea28fec6ba794`.
  WebM SHA-256: `a71ce31ef358bee172f8f9ec5fcf33103c81e798d7068372dd64356a99991aec`.
- Local production build, browser playback/seek/replay, 390px layout and
  reduced-motion checks pass. The opening Cloud film and carousel are preserved.
- Production is aliased to [omadesign.app](https://omadesign.app), deployment
  [`dpl_2n66W85NzsEGYWou87D9HK4WcWAs`](https://omadesign-6xntevi2r-hustle-launch.vercel.app).
  Homepage, release notes, installer, docs, skill and all versioned film/branding
  assets return HTTP 200. Public film and logo bytes match their source hashes;
  both video formats support HTTP 206 range requests. Both release download links
  resolve and the docs manifest identifies 0.5.7.
- On the deployed page the 32-second WebM plays, seeks to 15 seconds, ends and
  replays without media errors. Desktop/mobile screenshots verify the official
  header/footer marks and film. At 390px there is no horizontal overflow; reduced
  motion leaves videos paused. Production browser and runtime error logs are empty.
- The local production build and live deployment pass. The separate Blacksmith
  website CI run remained queued during publication and is not counted as a pass.

## Evidence and policy

Persistent evidence: `/home/michael/Documents/Omadesign-QA-0.5.7-20260921/`.
Desktop compilation, tests and packaging run locally; Blacksmith is website-only.
The accepted 0.5.0 human-QA evidence remains unchanged: Michael confirmed
“Qa passed” on 2026-09-12 for SHA-256
`69c35f6cc3f4397a883798172a0d654cb05289e4715d0678db7822c823e6791d`.
Automated checks for this patch do not claim new human acceptance.
