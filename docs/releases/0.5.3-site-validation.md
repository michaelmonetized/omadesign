# 0.5.3 website build and deployment validation

Status: **production deployed and live checks passed**.

On 2026-09-20 the nightly-based 0.5.3 tree passed its frozen Bun install,
Vercel production build, and TypeScript check. Production environment
variables and project settings were pulled through the existing authenticated
Vercel project link; secret files remain gitignored.

The final production build used:

```sh
cd site
vercel pull --yes --environment=production --scope hustle-launch
mise exec bun@1.4.2 -- vercel build --prod --scope hustle-launch
```

Prepared output: `site/.vercel/output` (Build Output API version 3).
Validation confirmed 59 static files, the 0.5.3 release and update post,
an installer identical to `scripts/install-remote.sh`, and absence of
server secret values in the client bundles. Local log:
`/tmp/omadesign-0.5.3-production-prebuilt.log`.

After the final GitHub release assets were published, the reviewed output
was deployed using:

```sh
vercel deploy --prebuilt --prod --archive=tgz --yes --scope hustle-launch
```

## Existing hosted checks

- [Blacksmith site CI run 35516345197](https://github.com/michaelmonetized/omadesign/actions/runs/35516345197) remained queued with zero executed steps during this checkpoint. It is not a pass. Cargo builds and tests remain local.
- [PR 77 Vercel preview](https://vercel.com/hustle-launch/omadesign/5EoUdV4QaLBqRghY5FCkmCsyy7HE) failed before compilation. Build logs show the Git integration ran `bun install --frozen-lockfile` in the repository root, which contains no `package.json`. The configured Vercel `rootDirectory` is null; the web app lives in `site/`.
- Clerk/Convex environment variables are configured for production, with no preview target. This is separate from the observed preview install failure.
- Existing Vercel project settings and workflow configuration were left unchanged. Building from the linked `site/` directory with production settings succeeds.
- The optional GitHub Pages static build rendered the new update route but failed its existing account `/cloud` link check under the `/omadesign/` prefix. It was not published. Production uses the validated Vercel output.

## Publication evidence

- Deployment: `dpl_DZqapqhxGEx5b62dikBwGLh4KAPT`.
- State/target: **READY**, **production**, verified with Vercel inspect.
- Deployment URL: <https://omadesign-jgcodp1ge-hustle-launch.vercel.app>.
- Production alias: <https://omadesign.app>.
- Release: <https://github.com/michaelmonetized/omadesign/releases/tag/v0.5.3>.
- No Vercel project settings or workflow configuration were changed.

Live HTTP checks returned **200** for `/`, `/updates`, `/updates/0.5.3`,
`/install`, `/cloud`, and `/docs/manual`. The homepage exposes the exact
0.5.3 ARM64 and x86_64 archive links; both download URLs returned 200.
The update index and version page show **Paste it onto the canvas**.

The live installer is served as `text/plain; charset=utf-8` and exactly
matches `scripts/install-remote.sh`:

```text
b4487e923c48eaac915b5762a2261a44ec997204267985b124801a65fba0ed01
```

The public checksum files resolve to the final release archives:

```text
ARM64  517516040e5b8abed28b6ea0b1ef07aaed9578d5309ad63a99614e7308b2c837
x86_64 dfd49bdd9250741235ef5456b52b4544269b3a280de27411182a9d825d7673b0
```

A deployment-scoped Vercel runtime error query covering the first live
checks returned zero error entries. The `/cloud` check establishes a
working public route; it is not a new authenticated cloud workflow test.

Durable evidence is under
`/home/michael/.local/share/omadesign/qa/0.5.3/site-production/`: HTTP
responses, `live-checks.json`, `download-checks.json`, production build and
deploy logs, Vercel inspect output, and the empty runtime error result.
